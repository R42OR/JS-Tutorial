const a = 23
const b = 32
const first = "Rajat"
const last = "Sinha"

addTwo = () => ( a + b)
console.log(addTwo())

addString = (str1, str2) => (
        console.log(`${str1}  ${str2}`)
)

addTwo()
addString(first,last)